# Graph Methods

- Graph.init(a)
- Graph.isFillState(h)
- Graph.isGradientState(h)
- Graph.isStrokeState(h)
- Graph.getTextColor(h, m)
- Graph.isSpecialColor(h)
- Graph.isGlassState(h)
- Graph.isRoundedState(h)
- Graph.isLineJumpState(h)
- Graph.isAutoSizeState(h)
- Graph.isImageState(h)
- Graph.isShadowState(h)
- Graph.getVerticesAndEdges(h, m)
- Graph.applyNewEdgeStyle(h, m, p)
- Graph.getCommonStyle(h)
- Graph.mergeStyle(h, m, p)
- Graph.getStartEditingCell(h, m)
- Graph.copyStyle(h)
- Graph.pasteStyle(h, m, p, k)
- Graph.copyCellStyles(h, m, p, k, u, w, y)
- Graph.includeDescendants(h)
- Graph.updateCellStyles(h, m)
- Graph.isFastZoomEnabled()
- Graph.isCssTransformsSupported()
- Graph.getScaledCellAt(h, m, p, k, u, w)
- Graph.isRecursiveVertexResize(h)
- Graph.getAbsoluteParent(h)
- Graph.isPart(h)
- Graph.getCompositeParents(h)
- Graph.getReferenceTerminal(h)
- Graph.getCompositeParent(h)
- Graph.filterSelectionCells(h)
- Graph.scrollRectToVisible(h)
- Graph.getCellsForGroup(h)
- Graph.getCellsForUngroup(h)
- Graph.updateCssTransform()
- Graph.isLightboxView()
- Graph.isViewer()
- Graph.labelLinkClicked(a, b, f)
- Graph.openLink(a, b, f)
- Graph.getLinkTitle(a)
- Graph.isCustomLink(a)
- Graph.customLinkClicked(a, b)
- Graph.isExternalProtocol(a)
- Graph.isBlankLink(a)
- Graph.isRelativeUrl(a)
- Graph.getAbsoluteUrl(a)
- Graph.initLayoutManager()
- Graph.createLayouts(a)
- Graph.getDataForCells(a, b)
- Graph.getNodesForCells(a)
- Graph.createWipeAnimations(a, b)
- Graph.createEdgeWipeAnimation(a, b)
- Graph.createVertexWipeAnimation(a, b)
- Graph.executeAnimations(a, b, f, e)
- Graph.getPageSize()
- Graph.getPageLayout(a, b, f)
- Graph.getDefaultTranslate(a)
- Graph.updateMinimumSize()
- Graph.sanitizeHtml(a, b)
- Graph.updatePlaceholders()
- Graph.isReplacePlaceholders(a)
- Graph.isZoomWheelEvent(a)
- Graph.isScrollWheelEvent(a)
- Graph.isTransparentClickEvent(a)
- Graph.isIgnoreTerminalEvent(a)
- Graph.isEdgeIgnored(a)
- Graph.isSplitTarget(a, b, f)
- Graph.getLabel(a)
- Graph.isLabelMovable(a)
- Graph.setGridSize(a)
- Graph.setDefaultParent(a)
- Graph.getClickableLinkForCell(a)
- Graph.getGlobalVariable(a)
- Graph.formatDate(a, b, f)
- Graph.getLayerForCell(a)
- Graph.getLayerForCells(a)
- Graph.createLayersDialog(a, b)
- Graph.replacePlaceholders(a, b, f, e)
- Graph.restoreSelection(a)
- Graph.selectCellForEvent(a, b)
- Graph.selectTableRange(a, b)
- Graph.snapCellsToGrid(a, b)
- Graph.removeChildCells(a)
- Graph.updateShapes(a, b, f)
- Graph.selectCellsForConnectVertex(a, b, f)
- Graph.isCloneConnectSource(a)
- Graph.insertEdgeBeforeCell(a, b)
- Graph.connectVertex(a, b, f, e, g, d, h, m)
- Graph.getIndexableText(a)
- Graph.convertValueToString(a)
- Graph.getLinksForState(a)
- Graph.getLinkForCell(a, b)
- Graph.getLinkTargetForCell(a)
- Graph.postProcessCellStyle(a, b)
- Graph.updateHorizontalStyle(a, b)
- Graph.replaceDefaultColors(a, b)
- Graph.replaceDefaultColor(a, b, f)
- Graph.getDefaultColor(a, b, f)
- Graph.setAdaptiveColors(a)
- Graph.getAdaptiveColors()
- Graph.updateAlternateBounds(a, b, f)
- Graph.moveCellsTo(a, b)
- Graph.isMoveCellsEvent(a, b)
- Graph.foldCells(a, b, f, e, g)
- Graph.moveSiblings(a, b, f, e)
- Graph.resizeParentStacks(a, b, f, e)
- Graph.isContainer(a)
- Graph.isCellConnectable(a)
- Graph.isLabelMovable(a)
- Graph.selectAll(a)
- Graph.selectCells(a, b, f)
- Graph.getSwimlaneAt(a, b, f)
- Graph.isCellFoldable(a)
- Graph.reset()
- Graph.zoom(a, b)
- Graph.zoomIn()
- Graph.zoomOut()
- Graph.fitPages(a, b)
- Graph.fitWindow(a, b)
- Graph.convertValueToTooltip(a)
- Graph.getTooltipForCell(a)
- Graph.addFlowAnimationToNode(a, b, f, e)
- Graph.addFlowAnimationStyle()
- Graph.createFlowAnimationCss(a)
- Graph.stringToBytes(a)
- Graph.bytesToString(a)
- Graph.compressNode(a)
- Graph.compress(a, b)
- Graph.decompress(a, b)
- Graph.zapGremlins(a)
- Graph.removeTextStyleForCell(a, b)
- Graph.createParent(a, b, f, e, g)
- Graph.createTable(a, b, f, e, g, d, h, m, p)
- Graph.setTableValues(a, b, f)
- Graph.createCrossFunctionalSwimlane(a, b, f, e, g, d, h, m, p)
- Graph.visitTableCells(a, b)
- Graph.getTableLines(a, b, f)
- Graph.isTableCell(a)
- Graph.isTableRow(a)
- Graph.isTable(a)
- Graph.isStack(a)
- Graph.isStackChild(a)
- Graph.setTableRowHeight(a, b, f)
- Graph.setTableColumnWidth(a, b, f)
- Graph.createCurrentEdgeStyle()
- Graph.getPagePadding()
- Graph.loadStylesheet()
- Graph.createCellLookup(q, A)
- Graph.createCellMapping(q, A, C)
- Graph.encodeCells(q)
- Graph.isCellEditable(q)
- Graph.isExtendParent(q)
- Graph.splitEdge(q, A, C, F, L, R, Z, ja)
- Graph.selectCell(q, A, C)
- Graph.swapShapes(q, A)
- Graph.moveCells(q, A, C, F, L, R, Z)
- Graph.removeCells(q, A)
- Graph.updateCustomLinks(q, A, C)
- Graph.updateCustomLinksForCell(q, A)
- Graph.doUpdateCustomLinksForCell(q, A) {};
- Graph.getAllConnectionConstraints(q, A)
- Graph.flipEdge(q)
- Graph.isValidRoot(q)
- Graph.isValidDropTarget(q, A, C)
- Graph.createGroupCell()
- Graph.isExtendParentsOnAdd(q)
- Graph.getPreferredSizeForCell(q, A, C)
- Graph.turnShapes(q, A)
- Graph.stencilHasPlaceholders(q)
- Graph.processChange(q)
- Graph.invalidateDescendantsWithPlaceholders(q)
- Graph.replaceElement(q, A)
- Graph.processElements(q, A)
- Graph.updateLabelElements(q, A, C)
- Graph.cellsRemoved(q)
- Graph.removeCellsAfterUngroup(q)
- Graph.setTooltipForCell(q, A)
- Graph.getAttributeForCell(q, A, C)
- Graph.isTargetShape(q, A, C)
- Graph.getDropTarget(q, A, C, F)
- Graph.dblClick(q, A)
- Graph.insertTextForEvent(q, A)
- Graph.getFreeInsertPoint()
- Graph.getCenterInsertPoint(q)
- Graph.isMouseInsertPoint()
- Graph.appendFontSize(q, A)
- Graph.addText(q, A, C)
- Graph.addClickHandler(q, A, C)
- Graph.duplicateCells(q, A)
- Graph.insertImage(q, A, C)
- Graph.insertLink(q)
- Graph.distributeCells(q, A, C)
- Graph.isCloneEvent(q)
- Graph.parseBackgroundImage(q)
- Graph.getBackgroundImageObject(q)
- Graph.getSvg(q, A, C, F, L, R, Z, ja, ea, ka, xa, Ka, Ha, ya, Fa, va)
- Graph.addForeignObjectWarning(q, A)
- Graph.disableSvgLinks(q, A)
- Graph.createSvgCanvas(q)
- Graph.getSelectedElement()
- Graph.getSelectedEditingElement()
- Graph.getParentByNames(q, A, C)
- Graph.selectNode(q)
- Graph.flipEdgePoints(q, A, C)
- Graph.flipChildren(q, A, C)
- Graph.flipCells(q, A)
- Graph.deleteCells(q, A)
- Graph.deleteLane(q)
- Graph.insertLane(q, A)
- Graph.insertTableRow(q, A)
- Graph.deleteTableColumn(q)
- Graph.insertRow(q, A)
- Graph.deleteRow(q, A)
- Graph.insertColumn(q, A)
- Graph.deleteColumn(q, A)
- Graph.pasteHtmlAtCaret(q)
- Graph.createLinkForHint(q, A, C)
- Graph.initTouch()
- Graph.isTouchPopupMenuEnabled()
- Graph.createEdgeHandler(c, l)
- Graph.init()
- Graph.postProcessCellStyle(l, t)
- Graph.createTagsDialog(l, t, D, M)
- Graph.getCustomFonts()
- Graph.setFont(l, t)
- Graph.isFastZoomEnabled()
- Graph.updateGlobalUrlVariables()
- Graph.getExportVariables()
- Graph.getGlobalVariable(l)
- Graph.getDefaultStylesheet()
- Graph.isViewer()
- Graph.getSvg(l, t, D, M, L, O, Q, S, V, X, W, Y, Z, aa, ha, ea)
- Graph.refresh()
- Graph.refreshBackgroundImage()
- Graph.loadStylesheet()
- Graph.handleCustomLink(l, t)
- Graph.executeCustomActions(l, t, D)
- Graph.doUpdateCustomLinksForCell(l, t)
- Graph.updateCustomLink(l, t)
- Graph.updateCustomLinkActions(l, t)
- Graph.updateCustomLinkAction(l, t, D)
- Graph.getCellsForAction(l, t)
- Graph.getCellsById(l)
- Graph.isCellVisible(l)
- Graph.setHiddenTags(l)
- Graph.toggleHiddenTag(l)
- Graph.isAllTagsHidden(l)
- Graph.getCellsForTags(l, t, D, M)
- Graph.getAllTags()
- Graph.getCommonTagsForCells(l)
- Graph.getTagsForCells(l)
- Graph.getTagsForCell(l)
- Graph.addTagsForCells(l, t)
- Graph.removeTagsForCells(l, t)
- Graph.toggleCells(l)
- Graph.setCellsVisible(l, t)
- Graph.highlightCells(l, t, D, M)
- Graph.highlightCell(l, t, D, M, L)
- Graph.addSvgShadow(l, t, D, M)
- Graph.updateShadowFilter(l, t)
- Graph.setShadowVisible(l, t)
- Graph.checkDefaultParent()
- Graph.selectUnlockedLayer()
- Graph.createViewState(b)
- Graph.saveViewState(b, f, k, c, p)
- Graph.getViewState()
- Graph.setViewState(b, f)
- Graph.setScrollbarPositions(b, f, k)
- Graph.addExtFont(b, f, k)
- Graph.foldCells(c, p, q, u, y)
- Graph.foldTreeCell(c, p)
- Graph.isTreeEdge(c)
- Graph.getTreeEdges(c, p, q, u, y, H)
- Graph.getIncomingTreeEdges(c, p)
- Graph.getOutgoingTreeEdges(c, p)
- Graph.isLightboxView()
