# App Methods

- App.initializeEmbedMode()
- App.initializeViewerMode()
- App.init()
- App.scheduleSanityCheck()
- App.stopSanityCheck()
- App.sanityCheck()
- App.isOwnGDriveDomain()
- App.isDriveDomain()
- App.getPusher()
- App.searchHelp(b)
- App.showNameConfBanner()
- App.showDownloadDesktopBanner()
- App.showRatingBanner()
- App.checkLicense()
- App.handleLicense(b, f) {};
- App.getEditBlankXml()
- App.updateActionStates()
- App.addRecent(b, f, k)
- App.getRecent(b)
- App.resetRecent(b)
- App.onBeforeUnload()
- App.updateDocumentTitle()
- App.getThumbnail(b, f, k)
- App.setMode(f, k)
- App.appIconClicked(b)
- App.clearMode()
- App.open()
- App.loadGapi(b)
- App.load()
- App.showRefreshDialog(b, f)
- App.showAlert(b)
- App.start()
- App.openTemplateDialog(b)
- App.generateDiagram(b, f, k)
- App.loadDraft(b, f)
- App.filterDrafts(b, f, k)
- App.checkDrafts()
- App.showSplash(b)
- App.addLanguageMenu(b, f, k)
- App.loadFileSystemEntry(b, f, k)
- App.createFileSystemOptions(b)
- App.showSaveFilePicker(b, f, k)
- App.pickFile(b)
- App.pickLibrary(b)
- App.saveLibrary(b, f, k, c, p, q, u)
- App.saveFile(b, f)
- App.loadTemplate(b, f, k, c, p)
- App.getModeForChar(b)
- App.isModeEnabled(b)
- App.isModeReady(b)
- App.uncompressPages(b)
- App.createFile(b, f, k, c, p, q, u, y, H, E)
- App.fileCreated(b, f, k, c, p, q)
- App.loadFile(b, f, k, c, p)
- App.getLibraryStorageHint(b)
- App.restoreLibraries()
- App.loadLibraries(b, f)
- App.updateButtonContainer()
- App.fetchAndShowNotification(b, f)
- App.showNotification(b, f)
- App.save(b, f)
- App.getExtensionForService(b)
- App.getServiceForName(b)
- App.getTitleForService(b)
- App.pickFolder(b, f, k, c, p, q)
- App.exportFile(b, f, k, c, p, q)
- App.descriptorChanged()
- App.showAuthDialog(b, f, k, c)
- App.convertFile(b, f, k, c, p, q, u, y)
- App.updateHeader()
- App.setCompactMode(b, f, k)
- App.getMainUser()
- App.hideUserPanel()
- App.toggleUserPanel()
- App.getCurrentUser()
